I"G(function() {
  var board_size, construct_game;

  board_size = 4;

  window.change = function() {
    return $('#test_button').html("coffee function");
  };

  construct_game = function() {
    var i, num, ref;
    for (num = i = 1, ref = board_size; 1 <= ref ? i <= ref : i >= ref; num = 1 <= ref ? ++i : --i) {
      console.log(num);
    }
    jQuery('<p />', {
      id: 'test'
    }).appendTo($('#game_zone'));
    return $('#test').html("Writing in a brand new div !");
  };

  console.log("ah que coucou from coffee");

  $('document').ready(construct_game);

}).call(this);
:ET